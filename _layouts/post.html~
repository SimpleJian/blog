---
layout: default
---
<div id="post">
{{ content }}
</div>

<div id="related">
  <ul class="posts">
    {% if page.previous %}
      <li><span>上一篇</span> &raquo; <a href="{{ site.baseurl }}{{ page.previous.url }}">{{ page.previous.title | truncatewords:5 }}</a></li>
    {% endif %}
    {% if page.next %}
      <li><span>下一篇</span> &raquo; <a href="{{ site.baseurl }}{{ page.next.url }}">{{ page.next.title | truncatewords:5 }}</a></li>
    {% endif %}
  </ul>
  <h2>Related Posts</h2>
  <ul class="posts">
    {% for post in site.related_posts limit:3 %}
      <li><span>{{ post.date | date_to_string }}</span> &raquo; <a href="{{ site.baseurl }}{{ post.url }}">{{ post.title }}</a></li>
    {% endfor %}
  </ul>
</div>

